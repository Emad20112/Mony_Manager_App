# دليل استخدام Device Preview

## 📱 ما هو Device Preview؟

`device_preview` هي مكتبة Flutter تسمح لك بتشغيل التطبيق على أجهزة مختلفة (iPhone, iPad, Android) داخل بيئة التطوير دون الحاجة إلى أجهزة فعلية.

## ✅ المتطلبات

- تم إضافة المكتبة في `pubspec.yaml`
- تم تحديث `main.dart` لاستخدام Device Preview

## 🚀 خطوات التشغيل

### 1. تثبيت المكتبات

```bash
flutter pub get
```

### 2. تشغيل التطبيق

```bash
flutter run
```

أو من Android Studio/VS Code:
- اضغط على زر Run (▶️)
- أو استخدم `F5` في VS Code

### 3. استخدام Device Preview

عند تشغيل التطبيق في وضع Debug، ستظهر نافذة Device Preview تلقائياً مع:

#### أ) اختيار الجهاز
- اضغط على أيقونة الجهاز في الزاوية العلوية
- اختر من القائمة:
  - **iPhone 14 Pro** (أو أي iPhone)
  - **iPhone 14 Pro Max**
  - **iPad Pro**
  - **Android Phone/Tablet**
  - وغيرها من الأجهزة

#### ب) تغيير الاتجاه
- استخدم زر Rotate (🔄) لتدوير الشاشة
- أو استخدم `Ctrl + R` (Windows/Linux) أو `Cmd + R` (Mac)

#### ج) تغيير اللغة
- استخدم قائمة Language لاختبار التطبيق بلغات مختلفة

#### د) تغيير الثيم
- استخدم قائمة Theme لاختبار الوضع الفاتح/الداكن

#### هـ) التقاط لقطات الشاشة
- استخدم زر Screenshot لالتقاط صور للواجهات

#### و) عرض معلومات الجهاز
- اضغط على أيقونة Info لرؤية معلومات الجهاز المحاكى

## ⚙️ الإعدادات المتقدمة

### تعطيل Device Preview في وضع الإنتاج

Device Preview **مُعطّل تلقائياً** في وضع الإنتاج (`kReleaseMode`). هذا يعني:
- ✅ في Debug Mode: Device Preview يعمل
- ❌ في Release Mode: Device Preview معطل (لأداء أفضل)

### تغيير الإعدادات الافتراضية

يمكنك تعديل الإعدادات في `main.dart`:

```dart
DevicePreview(
  enabled: !kReleaseMode, // تعطيل في الإنتاج
  builder: (context) => const MoneyManagerApp(),
  // إعدادات إضافية:
  // defaultDevice: Devices.iphone14Pro,
  // storage: DevicePreviewStorage.none(),
  // style: DevicePreviewStyle(
  //   background: DevicePreviewBackground.solid(Colors.black),
  // ),
)
```

## 📋 الأجهزة المدعومة

Device Preview يدعم العديد من الأجهزة:

### iPhone
- iPhone SE
- iPhone 12/13/14/15 (Pro, Pro Max, Plus)
- iPhone X/XS/XR

### iPad
- iPad Pro (جميع الأحجام)
- iPad Air
- iPad Mini

### Android
- Pixel 6/7/8
- Samsung Galaxy S21/S22/S23
- Nexus 5X/6P
- وغيرها

## 🎯 نصائح للاستخدام

1. **اختبار التصميم المتجاوب**: استخدم أجهزة مختلفة لاختبار كيف يبدو التطبيق على شاشات مختلفة
2. **اختبار الاتجاهات**: تأكد من أن التطبيق يعمل بشكل صحيح في الوضع العمودي والأفقي
3. **اختبار الألوان**: استخدم Theme Switcher لاختبار الوضع الفاتح والداكن
4. **التقاط لقطات**: استخدم Screenshot لالتقاط صور للتوثيق

## ⚠️ ملاحظات مهمة

- Device Preview يعمل فقط في **Debug Mode**
- بعض الميزات (مثل الكاميرا، GPS) قد لا تعمل بشكل كامل في المحاكي
- الأداء قد يكون أبطأ قليلاً من التشغيل العادي
- للتطبيقات الكبيرة، قد يستغرق التحميل وقتاً أطول

## 🔧 حل المشاكل

### Device Preview لا يظهر
- تأكد من أنك في **Debug Mode** وليس Release Mode
- أعد تشغيل التطبيق: `flutter run`

### التطبيق لا يعمل بشكل صحيح
- تأكد من تحديث المكتبات: `flutter pub get`
- امسح الكاش: `flutter clean` ثم `flutter pub get`

### الأخطاء في Console
- تأكد من أن جميع المكتبات محدثة
- تحقق من أن `device_preview` موجودة في `pubspec.yaml`

## 📚 مراجع إضافية

- [Device Preview Documentation](https://pub.dev/packages/device_preview)
- [Flutter Documentation](https://flutter.dev/docs)

---

**تم التحديث:** تم إعداد `main.dart` لاستخدام Device Preview بنجاح! 🎉

